global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: vulnerability-manager-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]
        labels:
          service: vulnerability-manager
          environment: docker
